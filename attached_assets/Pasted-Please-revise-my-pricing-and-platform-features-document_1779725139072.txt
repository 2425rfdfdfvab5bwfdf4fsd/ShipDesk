Please revise my pricing and platform features document to fix the following issues. Keep the existing structure but address each problem clearly:

1. Clarify the trial flow (pick ONE): Decide whether the 14-day Starter trial is (a) auto-started when a user creates a workspace, or (b) manually started by clicking "Try free for 14 days" on the pricing page. Update both the pricing card text and the Trial & Access Rules section so they match.

2. Add the "Remaining Days" feature: Under a new section called "Billing & Subscription Visibility", document that users will see their remaining subscription days (e.g., "18 of 30 days remaining") on:

The Billing page in the dashboard
The Plan tab in Settings Apply this to all paid plans (Starter, Solo, Agency) AND show "X of 14 trial days remaining" during the free trial.
3. Document the Starter button state logic: Specify that the Starter plan card button shows "Try free for 14 days" for first-time users, and automatically switches to "Get started" after the trial has been used or if the user is no longer eligible for a trial.

4. Standardize formatting:

Make all plan feature lists use the same bullet style.
Move "Everything in Solo" to the top of the Agency feature list.
Use a consistent label for unreleased features (use "coming soon" everywhere, remove "v1.1" references from user-facing content).
5. Add billing edge cases under a new "Subscription Management" section:

What happens when payment fails (grace period? immediate lockout?)
Cancellation and refund policy
Upgrade/downgrade behavior (prorated? next cycle?)
Whether annual billing is offered
6. Clarify paywall behavior: When a trial expires without a paid plan, specify exactly:

What the developer can still access (read-only? nothing?)
Whether existing client portals stay live for clients
How long data is retained before deletion
7. Define admin override: Clarify that "admin-granted plans" refers to platform-level admins (ShipDesk staff) who can manually grant plan access for partnerships, beta testers, or support cases — and that workspace admins cannot grant this.

8. Separate tech stack from user-facing copy: Move implementation details (Gemini 1.5 Pro, Lemon Squeezy, Cloudinary, Tiptap, Brevo/Resend) into a separate "Technical Stack" section at the bottom, so the Platform Features section reads cleanly for end users.

Please return the revised document with all sections properly organized and these issues resolved.